# 2026-08-19 서버사이드/클라이언트사이드 아키텍처

## GitHub Pages 아키텍처

GitHub Pages는 정적 파일 호스팅 서비스임. 리포지토리 자체가 서버 역할을 하는 게 아니라, 빌드 결과물만 별도로 분리해서 배포하는 구조임.

**요청 흐름**
1. push → GitHub 빌드 파이프라인 트리거 (Jekyll 빌드 또는 순수 정적 파일이면 빌드 스킵)
2. 결과물이 별도의 정적 파일 스토리지에 저장됨
3. CDN(Fastly)을 통해 서빙됨

Git 프로토콜이나 GitHub API 서버는 이 요청 경로에 관여하지 않음. 완전히 분리된 서빙 레이어이기 때문에 리포지토리 트래픽과 git clone/push 성능은 서로 영향 없음.

**빌드 파이프라인**
- `_config.yml` 감지 시 Jekyll 빌드 실행, 없으면 스킵하고 그대로 서빙
- GitHub Actions로 커스텀 빌드(React, Vue 등) 시 Actions가 아티팩트를 만들고 Pages 배포 API로 업로드, Jekyll 프로세스는 우회됨

**제약사항**
- 서버사이드 로직 불가 (함수 실행 환경 없음)
- 빌드 타임아웃, 사이트 용량(1GB), 대역폭(월 100GB 소프트 리밋) 존재 → 멀티테넌트 인프라 공유 구조
- Private repo도 Pages는 public 노출 가능 → 소스 관리 권한과 배포 콘텐츠 접근 권한이 분리되어 있음

## 서버사이드 로직의 범위

핵심 기준은 **요청이 들어올 때마다 서버가 코드를 실행하는가**임.

**안 되는 것 (GitHub Pages 기준)**
- 런타임 실행 환경 자체가 없음 (PHP, Python, Node.js 등)
- DB 연결
- 세션/쿠키 기반 인증, 로그인 시스템
- 폼 제출 서버 처리
- API 엔드포인트 (요청마다 달라지는 동적 응답)
- SSR(서버사이드 렌더링)

**되는 것 (경계선)**
- 클라이언트 사이드 JS 실행은 전부 허용 (브라우저에서 실행되므로 서버사이드 아님)
- 브라우저에서 외부 API를 직접 fetch하는 것 가능 (백엔드를 딴 곳에 두고 클라이언트에서 호출하는 구조는 일반적)
- 빌드 시점 API 호출 후 정적 JSON/HTML로 구워서 배포하는 것 가능 (Jamstack 패턴, 요청마다 실행되는 게 아니라 배포 시점 1회 실행이므로 정적 범주에 포함)

## 서버사이드 vs 클라이언트사이드 본질적 차이

표면적 차이는 "코드 실행 위치"지만, 본질적 차이는 **신뢰(trust)와 통제권(control)**임. 서버는 개발자 통제 하에 있어 사용자가 못 건드리지만, 클라이언트는 사용자 손 안에 있어 조작 가능하다는 전제로 설계해야 함.

| 구분 | 서버사이드 | 클라이언트사이드 |
|---|---|---|
| 실행 위치 | 서버 컴퓨터 | 사용자 브라우저 |
| 코드 노출 | 사용자에게 안 보임 | 개발자도구로 전부 열람 가능 |
| 신뢰 주체 | 서버 = 신뢰 가능 | 클라이언트 = 신뢰 불가 |
| 상태 저장 | 서버 DB에 영구 저장, 여러 사용자 공유 | 브라우저 세션에만 존재, 새로고침 시 소멸 |
| 민감정보 | API 키 등 안전 보관 가능 | 코드에 넣는 순간 노출됨 |
| 실행 컴퓨터 소유주 | 개발자의 컴퓨팅 자원 | 사용자의 컴퓨팅 자원 |
| 리소스 비용 부담 | 개발자 (요청 증가 시 서버비 증가) | 사용자 (배터리/CPU 사용, 개발자는 파일 전송만 하면 됨) |
| 실행 시점 | 요청마다 서버가 그때그때 실행 | 파일은 미리 만들어져 배포됨, 요청 시점엔 파일 전송만, 실행은 사용자 브라우저에서 이후 발생 |
| 확장(scale) 단위 | 서버 증설 필요 | CDN이 파일 복제만 하면 됨 |

**GitHub Pages와 렌더링 방식의 관계**: GitHub Pages는 정적 호스팅일 뿐이고, 그 안에 담긴 내용에 따라 CSR이 될 수도, 순수 정적 문서일 수도 있음. "어디서 빌드하는가", "어디서 렌더링하는가", "어디서 서빙하는가"는 서로 별개의 축임.

## 오픈 API 활용 시 백엔드가 필요한 경우

오픈API를 프론트엔드에서 바로 써도 되는 경우: API가 완전 public이고 인증 불필요, CORS 허용, 단순 데이터 표시만 하는 경우.

백엔드가 필요한 경우:
- **API 키 숨기기**: 키를 프론트엔드 코드에 넣으면 브라우저 개발자도구로 노출됨. 백엔드가 프록시 역할로 키를 숨기고 대신 호출
- **Rate limit 방어**: 백엔드에서 캐싱하여 외부 API 호출 횟수 절감
- **여러 API 조합/가공**: 여러 API를 프론트에서 각각 fetch하는 대신 백엔드에서 조합해서 응답
- **사용자별 상태 저장**: 좋아요, 검색 이력, 리뷰 작성 등 오픈API가 해결 못 해주는 사용자별 데이터는 DB 필요

## 클라이언트 사이드 판단의 한계

클라이언트 JS도 조건문 기반 판단(`if` 등)은 가능함. 문제는 "판단할 수 있는가"가 아니라 **그 판단이 신뢰할 수 있는 권위를 가지는가(authoritative)**임.

- **UX용 판단**: 비밀번호 형식 체크, 이메일 형식 검증 등 사용자 편의를 위한 즉각 피드백 → 클라이언트에서 처리해도 됨
- **권위 있는 판단**: 로그인 여부, 실제 재고 수량, 결제 금액 검증 등 → 반드시 서버에서 처리해야 함

이유: 판단 로직 자체는 클라이언트에서 돌려도, 그 판단에 쓰이는 데이터(변수 값 등)를 사용자가 개발자도구에서 조작할 수 있음. 판단 로직은 정상 작동해도 전제가 오염되면 잘못된 결과가 나옴. 그래서 폼 검증은 클라이언트(UX용)와 서버(최종 검증) 이중으로 처리하는 게 일반적임.

## 클라이언트 조작의 영향 범위

기본적으로 클라이언트 사이드 조작(개발자도구로 변수/함수 변경 등)은 해당 사용자의 세션에서만 영향을 미침. 다른 사용자나 원본 소스에는 영향 없음.

**예외**: 조작된 값을 서버로 전송했을 때 서버가 검증 없이 그대로 받아들이면 실제 시스템에 영향을 미침.

```
클라이언트: 가격 5000원으로 조작 → 서버에 "5000원 결제해줘" 요청 전송
서버가 클라이언트 값을 그대로 믿고 처리 → 실제로 5000원에 결제됨 (취약점)
서버가 자체 DB에서 진짜 가격을 재조회하여 검증 → 조작 무효화됨 (정상)
```

이것이 웹 보안 원칙 "Never trust the client(클라이언트를 절대 신뢰하지 마라)"의 근거임. SQL 인젝션, 가격 조작, 권한 우회 등 다수의 보안 사고가 서버가 이 원칙을 지키지 않아서 발생함.
