# SQLite와 서버형 DB 구조 비교

## SQLite의 동작 방식

SQLite는 별도 서버 프로세스 없이 애플리케이션에 라이브러리 형태로 내장되어 동작하는 RDBMS임. 데이터베이스 파일 하나(`app.db`)에 데이터, 인덱스, 스키마 정보가 전부 저장됨.

애플리케이션 프로세스가 OS 파일 시스템을 통해 DB 파일을 직접 읽고 씀. 네트워크 통신이 없음. 애플리케이션이 종료되면 SQLite 엔진도 함께 꺼짐(데이터는 파일에 남음).

### 적합한 상황
- 데모, POC, MVP 단계
- 읽기 작업이 쓰기 작업보다 많은 서비스
- 트래픽이 적은 내부용 툴이나 소규모 웹

### 한계
동시 쓰기(Concurrent Write) 제약이 핵심 한계임. 파일 단위 트랜잭션 락을 사용하기 때문에 여러 사용자가 동시에 쓰기 요청을 보내면 병목이 생김. WAL(Write-Ahead Logging) 모드로 전환하면 읽기와 쓰기를 어느 정도 병렬 처리할 수 있으나, 쓰기끼리의 동시성은 근본적으로 해결되지 않음.

## SQLite의 보안 취약점

백엔드와 동일 프로세스에 있어 네트워크를 통한 직접 침투(포트 스캔 등)로부터는 비교적 안전함. 하지만 구조 특성상 발생하는 취약점이 존재함.

1. **파일 시스템 침투 시 데이터 전체 노출**: DB가 하나의 파일이므로, 웹 쉘이나 LFI 취약점으로 파일 시스템에 접근하면 DB 파일 전체를 다운로드/삭제할 수 있음.
2. **SQL Injection**: SQLite도 표준 SQL을 지원하므로 문자열 결합 방식으로 쿼리를 작성하면 SQLi에 노출됨. 특정 확장 기능이 활성화된 경우 임의 파일 생성/조작으로 이어질 수 있음.
3. **DoS(파일 락)**: 시간이 오래 걸리는 쓰기 요청을 연속으로 보내 파일에 락을 걸면 다른 요청이 대기 상태에 빠짐.
4. **파일 접근 권한 설정 실수**: DB 파일을 웹 서버의 public/static 폴더에 두면 URL로 직접 다운로드 가능해짐.
5. **기본 암호화 미지원**: 표준 SQLite는 데이터를 평문으로 저장함. 암호화가 필요하면 SQLCipher 등 전용 확장 필요.

가장 근본적인 차이는 **DB 레벨 접근 제어의 부재**임. 서버형 DB는 계정별 권한 시스템(GRANT/REVOKE)이 있어 접근을 세밀하게 분리할 수 있지만, SQLite는 이런 개념 자체가 없어서 파일에 접근 가능한 OS 프로세스는 전체 데이터에 다 접근 가능함. 파일 유출은 이 부재의 결과 중 하나일 뿐임.

### 방어 수칙
- DB 파일을 Document Root 외부에 저장
- Prepared Statement / 파라미터화된 쿼리 사용
- WAL 모드 및 Busy Timeout 설정
- 백엔드 프로세스를 전용 계정으로 실행, DB 파일 권한을 타이트하게(600) 관리

## 서버형 DB의 저장 구조

서버형 DB는 독립된 서버 프로세스로 동작하며, 데이터는 디스크의 전용 디렉토리에 여러 엔진 파일로 분할 저장됨.

| DB 종류 | 서버 프로세스명 | 저장 엔진 | 파일 저장 형태 |
|---|---|---|---|
| MySQL | mysqld | InnoDB | `.ibd`(테이블 데이터/인덱스), `ibdata1`(시스템 테이블스페이스), `ib_logfile`(트랜잭션 로그) |
| PostgreSQL | postgres | 자체 통합 엔진 | `base/16384/16385` 등 OID 기반 숫자 파일명, WAL 로그 |
| MongoDB | mongod | WiredTiger | `.wt` 확장자 파일, Journal 로그 |

백엔드 애플리케이션은 이 파일들을 직접 다루지 않음. TCP 포트(MySQL: 3306, PostgreSQL: 5432, MongoDB: 27017)로 DB 서버 프로세스에 쿼리를 보내고, 실제 파일 읽기/쓰기·메모리 캐싱·락 관리는 DB 프로세스가 전담함.

### 동시성 제어 방식 차이
- MongoDB(WiredTiger): 문서(Document) 단위 락. 과거 컬렉션 전체에 락을 걸던 방식보다 병목이 적음.
- PostgreSQL: MVCC(Multi-Version Concurrency Control) 방식. 로우를 수정해도 이전 버전을 남겨두고 트랜잭션별로 다른 버전을 보게 함. 락 기반 제어와는 원리가 다름.

## WiredTiger

WiredTiger는 MongoDB의 저장 엔진임. MongoDB 프로세스가 쿼리를 해석한 뒤, 실제 디스크 I/O·압축·캐싱은 WiredTiger가 전담함. MongoDB 3.2부터 기본 엔진으로 채택됨(이전에는 MMAPv1 사용).

- 문서 단위 락으로 동시 읽기/쓰기 처리
- Snappy 알고리즘 기반 압축으로 저장 용량 60~80% 감소
- 자주 쓰는 데이터/인덱스를 RAM에 캐싱해 응답 속도 확보

## 서버형 구조가 필요한 이유

SQLite처럼 파일 내장 방식이 아니라 독립 프로세스 구조를 택하는 이유는 보안보다 성능과 확장성 때문임.

1. **동시성 처리**: 파일 전체 락이 아닌 로우/문서 단위 락으로 수만 명 동시 접근을 병렬 처리 가능.
2. **수평 확장**: 백엔드 서버를 여러 대로 늘려도 중앙 DB 서버(또는 클러스터)만 바라보면 데이터 일관성 유지 가능. 데이터량이 커지면 샤딩·리플리케이션으로 대응.
3. **메모리 캐싱**: DB 프로세스가 RAM을 대량 점유해 자주 조회되는 데이터를 캐싱함. 애플리케이션이 재시작돼도 DB 캐시는 유지됨.
4. **크래시 복구**: 디스크 기록 전 로그(Journal)에 먼저 기록을 남기고, 시스템 다운 시 손상된 트랜잭션을 복구(Rollback/Redo)함. 애플리케이션 코드가 오류로 튕겨도 별도 프로세스인 DB와 디스크 파일은 영향을 받지 않음.

## SQLite의 특이성

일반적인 데이터베이스는 클라이언트-서버 구조가 표준이고, SQLite는 이를 라이브러리 하나로 내장한 예외적인 사례임.

- 스마트폰(Android, iOS), 브라우저(Chrome, Safari), OS 자체 등에 광범위하게 내장되어 세계에서 가장 많이 설치된 데이터베이스임.
- 개발자 D. Richard Hipp이 서버 DB의 연결 불안정 문제를 겪은 뒤, 서버·네트워크 없이 실행 파일 하나에 들어가는 DB를 목표로 개발함.
- "Lite"라는 이름대로 서버 관리가 필요 없는(Serverless) 것이 정체성임.

초기 개발, MVP, 데모, 유틸리티 단계에서는 SQLite가 생산성 면에서 유리함. 서비스 규모가 커지고 동시 사용자·대용량 데이터 처리가 필요해지는 시점에 서버형 DB로 전환을 고려하는 것이 일반적인 흐름임.
